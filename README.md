<div align="center">

# 🚀 3X-UI Auto Installer

![Version](https://img.shields.io/badge/version-2.8.4-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Platform](https://img.shields.io/badge/platform-Ubuntu%20%7C%20Debian-orange.svg)

**Автоматическая установка и оптимизация 3X-UI панели одной командой**

[Установка](#-быстрая-установка) • [Оптимизации](#️-системные-оптимизации) • [Проверка](#-проверка-настроек)

</div>

---

## 📋 Что делает скрипт

Скрипт автоматически выполняет полную настройку VPN-панели 3X-UI с оптимизацией системы:

### 🔧 Установка компонентов
- **3X-UI панель** (версия 2.8.4) — веб-интерфейс управления VPN
- **Базовые утилиты** — curl, wget, unzip, socat, git, openssl, certbot
- **Мониторинг** — htop, net-tools, systemd
- **Безопасность** — fail2ban для защиты от брутфорса
- **SSL** — certbot с автопродлением сертификатов

### 💾 Настройка SWAP
- Создание файла подкачки 1GB
- Автоматическое добавление в fstab
- Проверка существующих настроек

### ⚡ Системная оптимизация
- TCP BBR алгоритм контроля перегрузки
- Увеличенные сетевые буферы
- Оптимизация параметров ядра
- Настройка лимитов файловых дескрипторов

### 🛡️ Безопасность
- Fail2Ban с защитой от подбора паролей
- Готовые скрипты для SSL/TLS сертификатов
- Защита от повторных неудачных попыток входа

---

## 🚀 Быстрая установка

### 📥 Способ 1: Через curl
```bash
curl -Ls https://raw.githubusercontent.com/CyberERROR/3X-UI-Auto-instal/main/3x-ui-auto.sh | sudo bash
```

### 📥 Способ 2: Через wget
```bash
wget -qO- https://raw.githubusercontent.com/CyberERROR/3X-UI-Auto-instal/main/3x-ui-auto.sh | sudo bash
```

### 📥 Способ 3: Скачать и запустить
```bash
wget https://raw.githubusercontent.com/CyberERROR/3X-UI-Auto-instal/main/3x-ui-auto.sh
chmod +x 3x-ui-auto.sh
sudo ./3x-ui-auto.sh
```

> ⚠️ **Важно:** Скрипт требует права root (sudo)

---

## ⚙️ Системные оптимизации

Скрипт применяет следующие оптимизации через `/etc/sysctl.d/99-3xui-optimization.conf`:

### 🌐 Сетевые оптимизации
| Параметр | Значение | Назначение |
|----------|----------|------------|
| `tcp_congestion_control` | bbr | Современный алгоритм контроля перегрузки |
| `tcp_fastopen` | 3 | Быстрое открытие TCP соединений |
| `rmem_max` / `wmem_max` | 128MB | Увеличенные буферы приема/отправки |
| `tcp_window_scaling` | 1 | Масштабирование TCP окна |
| `somaxconn` | 65535 | Максимум ожидающих соединений |
| `tcp_max_syn_backlog` | 8192 | Очередь SYN пакетов |

### 💾 Оптимизация памяти
| Параметр | Значение | Назначение |
|----------|----------|------------|
| `vm.swappiness` | 30 | Снижение использования SWAP |
| `vm.vfs_cache_pressure` | 50 | Баланс кэша файловой системы |
| `vm.dirty_ratio` | 15 | Контроль грязных страниц |

### 📂 Файловая система
| Параметр | Значение | Назначение |
|----------|----------|------------|
| `fs.file-max` | 2000000 | Максимум открытых файлов |
| `fs.nr_open` | 2000000 | Лимит дескрипторов процесса |
| `fs.inotify.max_user_watches` | 524288 | Мониторинг файлов |

### 🔀 IP Forwarding
```bash
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
```

---

## ✅ Проверка настроек

<div align="center">

### 🎯 Убедитесь, что все настройки применились корректно

</div>

<table>
<tr>
<td width="50%">

### 🔍 **1. TCP BBR алгоритм**

**Команда проверки:**
```bash
sysctl net.ipv4.tcp_congestion_control
lsmod | grep tcp_bbr
```

**✅ Ожидаем увидеть:**
```plaintext
net.ipv4.tcp_congestion_control = bbr
tcp_bbr    20480  1
```

> 💡 BBR включен и загружен в ядро

</td>
<td width="50%">

### 💾 **2. SWAP файл подкачки**

**Команда проверки:**
```bash
free -h
swapon --show
```

**✅ Ожидаем увидеть:**
```plaintext
Swap:    1.0Gi    0B    1.0Gi

NAME       TYPE  SIZE  USED  PRIO
/swapfile  file   1G    0B    -2
```

> 💡 SWAP 1GB создан и активен

</td>
</tr>

<tr>
<td>

### 🌐 **3. Сетевые буферы**

**Команда проверки:**
```bash
sysctl net.core.rmem_max net.core.wmem_max
```

**✅ Ожидаем увидеть:**
```plaintext
net.core.rmem_max = 134217728
net.core.wmem_max = 134217728
```

> 💡 Буферы установлены в 128MB

</td>
<td>

### 📂 **4. Лимиты файловой системы**

**Команда проверки:**
```bash
sysctl fs.file-max
ulimit -n
```

**✅ Ожидаем увидеть:**
```plaintext
fs.file-max = 2000000
65535
```

> 💡 Лимит 2 млн файлов установлен

</td>
</tr>

<tr>
<td>

### ⚡ **5. TCP FastOpen**

**Команда проверки:**
```bash
sysctl net.ipv4.tcp_fastopen
```

**✅ Ожидаем увидеть:**
```plaintext
net.ipv4.tcp_fastopen = 3
```

> 💡 FastOpen активен (клиент+сервер)

</td>
<td>

### 🔀 **6. IP Forwarding**

**Команда проверки:**
```bash
sysctl net.ipv4.ip_forward net.ipv6.conf.all.forwarding
```

**✅ Ожидаем увидеть:**
```plaintext
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
```

> 💡 Маршрутизация IPv4/IPv6 включена

</td>
</tr>

<tr>
<td>

### 🔌 **7. Очередь соединений**

**Команда проверки:**
```bash
sysctl net.core.somaxconn net.ipv4.tcp_max_syn_backlog
```

**✅ Ожидаем увидеть:**
```plaintext
net.core.somaxconn = 65535
net.ipv4.tcp_max_syn_backlog = 8192
```

> 💡 Очереди оптимизированы для нагрузки

</td>
<td>

### ⚙️ **8. Служба 3X-UI**

**Команда проверки:**
```bash
systemctl status x-ui
```

**✅ Ожидаем увидеть:**
```plaintext
● x-ui.service - 3X-UI Panel
   Active: active (running)
   Loaded: loaded
```

> 💡 Панель запущена и работает

</td>
</tr>
</table>

---

## 📊 Быстрая проверка всех настроек

### 🚀 Одна команда для полной диагностики

```bash
echo -e "\n🔍 === TCP BBR ===" && sysctl net.ipv4.tcp_congestion_control && lsmod | grep tcp_bbr && \
echo -e "\n💾 === SWAP ===" && free -h | grep Swap && swapon --show && \
echo -e "\n🌐 === Сетевые буферы ===" && sysctl net.core.rmem_max net.core.wmem_max && \
echo -e "\n📂 === Файловые лимиты ===" && sysctl fs.file-max && \
echo -e "\n⚡ === TCP FastOpen ===" && sysctl net.ipv4.tcp_fastopen && \
echo -e "\n🔀 === IP Forwarding ===" && sysctl net.ipv4.ip_forward net.ipv6.conf.all.forwarding && \
echo -e "\n🔌 === Очереди ===" && sysctl net.core.somaxconn net.ipv4.tcp_max_syn_backlog && \
echo -e "\n⚙️  === 3X-UI Статус ===" && systemctl is-active x-ui && echo ""
```

### 🎯 Ожидаемый полный вывод

```plaintext
🔍 === TCP BBR ===
net.ipv4.tcp_congestion_control = bbr
tcp_bbr                20480  1

💾 === SWAP ===
Swap:          1.0Gi          0B       1.0Gi
NAME      TYPE SIZE USED PRIO
/swapfile file   1G   0B   -2

🌐 === Сетевые буферы ===
net.core.rmem_max = 134217728
net.core.wmem_max = 134217728

📂 === Файловые лимиты ===
fs.file-max = 2000000

⚡ === TCP FastOpen ===
net.ipv4.tcp_fastopen = 3

🔀 === IP Forwarding ===
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1

🔌 === Очереди ===
net.core.somaxconn = 65535
net.ipv4.tcp_max_syn_backlog = 8192

⚙️  === 3X-UI Статус ===
active
```

<div align="center">

### ✅ **Все параметры соответствуют ожидаемым — система готова к работе!**

</div>

---

## 🔐 Доступ к панели

После установки данные для входа сохраняются в:
```bash
cat /root/3xui-installation-report.txt
```

**Стандартный доступ:**
- URL: `http://YOUR_SERVER_IP:2053/`
- Логин: указан в отчете
- Пароль: указан в отчете

---

## 🔄 Обновление SSL сертификатов

Скрипт создает утилиту автопродления в `/usr/local/bin/renew-3xui-cert.sh`

### Использование:
```bash
/usr/local/bin/renew-3xui-cert.sh your-domain.com admin@your-domain.com
```

---

## 💡 Особенности скрипта

✨ **Умная установка** — проверяет существующие компоненты перед установкой  
🔒 **Идемпотентность** — можно запускать повторно без потери данных  
🚫 **Без Firewall** — не изменяет правила iptables/ufw  
📝 **Подробное логирование** — все действия записываются  
🎨 **Цветной вывод** — удобная визуализация процесса  

---

## 📌 Системные требования

- **ОС:** Ubuntu 20.04+ / Debian 10+
- **RAM:** минимум 512MB (рекомендуется 1GB+)
- **Права:** root или sudo
- **Архитектура:** x86_64, ARM64

---

<div align="center">

**Создано с ❤️ для быстрого развертывания VPN**

⭐ Поставьте звезду, если проект полезен!

</div>
